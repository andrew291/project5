Stop coding. We found that this project uses com.google.adk.agents.LlmAgent with `.model(model)` as a string, not an explicit LanguageModel bean.

Do not create or modify files.

Please inspect the actual module that runs our support agent, not only the adk_remote_agent_example.

Find every usage of:
- LlmAgent.builder()
- .model(
- AgentExecutor
- RequestHandler
- RequestHandlerDecorator
- DefaultRequestHandler
- AgentExecutor.Builder
- app.model
- @Bean("rootAgents")
- @Bean("a2aAgent")
- @Bean("agentExecutor")

Answer:
1. Which module is the real running support agent module?
2. Which Config.java or @Configuration class creates the real LlmAgent?
3. Is there any explicit LanguageModel or GeminiLanguageModel bean anywhere?
4. If no, where can we intercept shadow-mode metrics safely?
5. Can we intercept at RequestHandlerDecorator instead of LanguageModel?
6. Can we intercept before/after AgentExecutor execution?
7. Which existing class should be modified?
8. Which generated compression files should be deleted because they are in the wrong module?
9. Do not make changes.
